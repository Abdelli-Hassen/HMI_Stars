\chapter*{Appendices}
\addcontentsline{toc}{chapter}{Appendices}
\setcounter{section}{0}
\renewcommand{\thesection}{\Alph{section}}

\section{UML Diagram Templates (PlantUML)}
These templates describe the UML model definitions used in this report. You can paste the code blocks below directly into a PlantUML compiler to render the diagrams.

\subsection{Global Use Case Diagram Source Code}
\begin{lstlisting}[language=java]
@startuml
left to right direction
skinparam packageStyle rectangle
skinparam actorStyle awesome

actor "Client (Gerant)" as client
actor "Administrateur Cabinet" as admin

rectangle "Ecosysteme HMI Stars" {
  usecase "S'authentifier" as UC1
  usecase "Gerer les salaries" as UC2
  usecase "Soumettre le pointage" as UC3
  usecase "Demander un conge" as UC4
  usecase "Deposer des documents" as UC5
  usecase "Discuter en temps reel" as UC6
  usecase "Planifier des taches" as UC7
  usecase "Traiter les conges" as UC8
  usecase "Generer des avertissements" as UC9
}

client --> UC1
client --> UC2
client --> UC3
client --> UC4
client --> UC5
client --> UC6

admin --> UC1
admin --> UC6
admin --> UC7
admin --> UC8
admin --> UC9
@enduml
\end{lstlisting}

\subsection{Authentication Sequence Diagram Source Code}
\begin{lstlisting}[language=java]
@startuml
actor Utilisateur as user
participant "Application Flutter" as app
participant "Supabase Auth" as auth
database "Base de donnees" as db

user -> app: Saisir e-mail de recuperation
app -> auth: resetPasswordForEmail(email, redirectTo)
auth -> user: Envoi e-mail avec lien de conenterpriseation (token/code)
user -> app: Clic sur le lien dans le mail
app -> auth: Echange implicite de token / validation
auth -> db: Verifier & Mettre a jour session
db --> auth: OK
auth --> app: Session authentifiee active
app -> user: Afficher ecran de modification
user -> app: Saisir & valider nouveau mot de passe
app -> auth: updatePassword(newPassword)
auth --> app: Conenterpriseation succes
app -> user: Rediriger vers l'ecran de connexion
@enduml
\end{lstlisting}
