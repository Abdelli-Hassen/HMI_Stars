# DevLog: hmi_stars
* **📅 Date**: 2026-05-24
* **🏷️ Tags**: `#Project` `#DevLog` `#LogoUpload` `#PointageAPI` `#UIUX`

---

> 🎯 **Progress Summary**
> Completed a comprehensive integration phase between the Admin Platform and Mobile App. Main focus areas were building out a robust logo upload and distribution pipeline, improving avatar cache-busting to prevent stale images, completing the mobile pointage API connection, and stabilizing UI features.

### 🛠️ Execution Details & Changes
* **Enterprise Logo Upload & Cache-Busting (`Platforme` & `hmistarsmobile`)**:
  * ✅ Implemented `uploadEntrepriseLogo` in `PlatformDataService` which uploads to Supabase storage and retrieves a cache-busted public URL.
  * ✅ Refactored `EntrepriseDetailsPage` with dynamic logo handling and logo upload inside the settings/edit dialog.
  * ✅ Integrated `ValueKeys` on all avatar widgets to enforce live updates and prevent image caching bugs.

* **Pointage Retrieval & Salary API Integrations (`pointage_service.dart`)**:
  * ✅ Created `fetchPointagesForSalarieAndMonth` API bindings to fetch active employee attendance records for any selected month.
  * ✅ Integrated the new pointage service inside the mobile client views (`EmployeeDayList` and `PointagePage`) to fetch real data from the backend.

* **Stability & UX Polish**:
  * ✅ Implemented auto-retry mechanisms in `NotificationProvider` for handling Supabase realtime subscription errors gracefully.
  * ✅ Upgraded the archived employee UI list and refined the employee creation/edit dialog with file chips, PDF scanner support, and contract end handling.
  * ✅ Standardized common widgets like `AppHeader` to improve navigation flows and visual hierarchy.

### 📁 Files Modified
| File | Changes |
|------|---------|
| `platform_data_service.dart` | Added `uploadEntrepriseLogo` and `fetchPointagesForSalarieAndMonth` methods |
| `entreprise_details_page.dart` | Added logo management tools and edit settings dialogs |
| `models.dart` & `app_state.dart` | Wrote standard model properties and verified constructor definitions |
| `pointage_page.dart` & `employee_day_list.dart` | Hooked up attendance listings to the live Supabase database |
| `conge_service.dart` & `conges_page.dart` | Created initial structures for leave request lists |

### ⏭️ Next Steps
- [ ] Clean up redundant header notification elements on the mobile app.
- [ ] Personalize the dashboard greeting with the active manager's name.
- [ ] Remove unused Accès Rapide shortcuts grid to simplify the main view.
- [ ] Replace standard grey chat star icons with circular HMI logo badges.
